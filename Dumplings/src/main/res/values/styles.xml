<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="DumplingRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/rating_stars</item>
        <item name="android:minHeight">40sp</item>
        <item name="android:maxHeight">40sp</item>
    </style>
    <style name="DumplingCheckboxStyle" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:drawable">@drawable/dumpling_checkbox</item>
        <item name="android:background">@drawable/dumpling_checkbox</item>
        <!--<item name="android:maxHeight">36sp</item>-->
        <!--<item name="android:minHeight">36sp</item>-->
    </style>


    <style name="SubBar">
        <item name="android:background">#333</item>
        <item name="android:textColor">#f3f3f3</item>
        <item name="android:textSize">22sp</item>
        <item name="android:padding">8dp</item>

    </style>
</resources>
